@page
@model Client.Pages.Product.DetailsModel

@{
}

<h2>Details</h2>

@* <div class="container mt-4">
    <div class="row">

        <!-- Левая часть с изображением -->
        <div class="col-md-7">
            <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.Product.Image)" class="img-fluid" alt="@Model.Product.Name">
        </div>

        <!-- Правая часть с информацией -->
        <div class="col-md-5">
            <h2 class="fw-bold">@Model.Product.Name</h2>
            <p class="text-muted">@Model.Product.Category</p>

            <div class="mb-3">
                <span class="badge bg-secondary">@Model.Product.Material</span>
                <span class="badge bg-light text-dark">@Model.Product.Style</span>
            </div>

            <h4 class="fw-bold"> @Model.Product.Price ₽</h4>

            <button class="btn btn-dark w-100 my-2">Купить за @Model.Product.Price ₽</button>
            <button class="btn btn-outline-dark w-100">Добавить оформление</button>

            <p class="mt-3"><strong>Художник:</strong> @Model.Product.Artist</p>
        </div>
    </div>

    <!-- Блок "О работе" -->
    <div class="row mt-5">
        <div class="col">
            <h4>О работе</h4>
            <p><strong>Год создания:</strong> @Model.Product.Year</p>
            <p><strong>Размер:</strong> @Model.Product.Size</p>
            <p><strong>Категория и техника:</strong> @Model.Product.Material</p>
            <p><strong>Галерея:</strong> @Model.Product.Gallery</p>
            <p><strong>Город:</strong> @Model.Product.City</p>
        </div>
    </div>

    <!-- Блок "О художнике" -->
    <div class="row mt-5">
        <div class="col-md-2">
            <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.Product.ArtistImage)" class="rounded-circle img-fluid" alt="@Model.Product.Artist">
        </div>
        <div class="col-md-10">
            <h5>@Model.Product.Artist</h5>
            <p>@Model.Product.ArtistBio</p>
        </div>
    </div>
</div> *@